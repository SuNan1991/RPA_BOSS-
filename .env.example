# Session Encryption Key (IMPORTANT: Generate a secure key for production!)
# Use: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SESSION_ENCRYPTION_KEY=your-encryption-key-here-change-this-in-production

# Development Settings
DEBUG=True
HOST=0.0.0.0
PORT=8000

# CORS Settings
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]
