# 配置文件的使用

来源: https://drissionpage.cn/advance/ini

本库使用 ini 文件记录浏览器或 `Session` 对象的启动配置。便于配置复用，免于在代码中加入繁琐的配置信息。

默认情况下，页面对象启动时自动加载文件中的配置信息。

也可以在默认配置基础上用简单的方法再次修改，再保存到 ini 文件。

也可以保存多个 ini 文件，按不同项目需要调用。

---

## 配置文件示例

```ini
[paths]
download_path =
tmp_path =

[chromium_options]
address = 127.0.0.1:9222
browser_path = chrome
arguments = ['--no-default-browser-check', '--disable-suggestions-ui', '--no-first-run', '--disable-infobars', '--disable-popup-blocking', '--hide-crash-restore-bubble', '--disable-features=PrivacySandboxSettings4']
extensions = []
prefs = {'profile.default_content_settings.popups': 0, 'profile.default_content_setting_values': {'notifications': 2}}
flags = {}
load_mode = normal
user = Default
auto_port = False
system_user_path = False
existing_only = False

[session_options]
headers = {'user-agent': 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_12_6) AppleWebKit/603.3.8 (KHTML, like Gecko) Version/10.1.2 Safari/603.3.8', 'accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8', 'connection': 'keep-alive', 'accept-charset': 'GB2312,utf-8;q=0.7,*;q=0.7'}

[timeouts]
base = 10
page_load = 30
script = 30

[proxies]
http =
https =

[others]
retry_times = 3
retry_interval = 2
```

---

## 配置文件位置

默认配置文件存放在 DrissionPage 库 `_configs` 文件夹下，文件名为 `configs.ini`。

用户可另存其它配置文件，或从另存的文件读取配置，但默认文件的位置和名称不会改变。

---

## 配置节说明

### [paths]

路径相关配置：
- `download_path` - 下载文件保存路径
- `tmp_path` - 临时文件路径

### [chromium_options]

浏览器启动配置：
- `address` - 浏览器地址
- `browser_path` - 浏览器可执行文件路径
- `arguments` - 启动参数列表
- `extensions` - 插件列表
- `prefs` - 用户首选项
- `flags` - 实验性功能
- `load_mode` - 页面加载策略
- `user` - 用户配置
- `auto_port` - 是否自动分配端口
- `system_user_path` - 是否使用系统用户路径
- `existing_only` - 是否仅使用已打开的浏览器

### [session_options]

Session 相关配置：
- `headers` - 请求头信息

### [timeouts]

超时配置：
- `base` - 基础超时时间（秒）
- `page_load` - 页面加载超时时间（秒）
- `script` - 脚本执行超时时间（秒）

### [proxies]

代理配置：
- `http` - HTTP 代理
- `https` - HTTPS 代理

### [others]

其他配置：
- `retry_times` - 重试次数
- `retry_interval` - 重试间隔（秒）

---

## 使用配置文件

### 读取配置

```python
from DrissionPage import Chromium, ChromiumOptions

# 使用默认配置文件
browser = Chromium()

# 使用指定配置文件
co = ChromiumOptions(ini_path=r'D:\configs\my_config.ini')
browser = Chromium(co)
```

### 保存配置

```python
from DrissionPage import ChromiumOptions

co = ChromiumOptions()
co.set_browser_path(r'D:\chrome.exe')
co.save()  # 保存到当前读取的配置文件

# 或保存到指定文件
co.save(path=r'D:\configs\my_config.ini')
```

### 修改默认配置

```python
from DrissionPage import ChromiumOptions

# 保存到默认配置文件
co = ChromiumOptions()
co.auto_port(True)
co.save_to_default()
```

---

*文档获取时间: 2026-02-07*
